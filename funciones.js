// DECLARACIÓN DE FUNCIONES.

/*
Esto es lo básico, 
lo típico, 
lo que siempre se ha usado */

function suma(a, b) {   //usamos la palabra reservada function para indicar que eso es una función, luego indicamos el nombre de la función y los parámetros Osea, lo que necesita para funcionar, con lo que va a trabajar 
    return a + b;      // Dentro de las llaves va lo que se va a hacer con esos parámetros.
}

console.log(suma(2,3)); //La llamamos con el nombre de la función y pasándole los argumentos

